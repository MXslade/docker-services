version: "3.9"

services:
  mongo-db:
    networks:
      - local-shared-network
    image: mongo:7-jammy
    ports:
      - "27017:27017"
    volumes:
      - ./mongo-db-data:/data/db

networks:
  local-shared-network:
    external: true
